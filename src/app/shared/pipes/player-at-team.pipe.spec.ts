import { PlayerAtTeamPipe } from './player-at-team.pipe';

describe('PlayerAtTeamPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerAtTeamPipe();
    expect(pipe).toBeTruthy();
  });
});
